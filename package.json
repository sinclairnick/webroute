{
  "name": "root",
  "workspaces": [
    "docs",
    "packages/*"
  ],
	"scripts": {
		"build:packages": "turbo build --filter '@webroute/**'",
		"check": "turbo check"
	},
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
