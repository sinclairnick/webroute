{
  "name": "root",
  "workspaces": [
    "docs",
    "packages/*"
  ],
	"scripts": {
		"build:packages": "pnpm turbo build --filter '@webroute/**'"
	},
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
