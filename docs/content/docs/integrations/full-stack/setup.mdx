---
title: Setup
---

Webroute is particularly suited for full-stack or meta-frameworks like `Next.js`, `Remix`, `SolidStart` and `SvelteKit`.

By default, these frameworks offer a minimal and unopinionated approach to building and using APIs. When reaching for more complex functionality, we are typically forced to use a monolithic framework like `Hono` or `Express`. However, this defeats the purpose of

While this guide focusses on `Next.js`, it can be easily applied to the others will a few obvious modifications.

## Benefits

Using webroute with your full-stack

## Prerequisites

Assuming you've already set up your full-stack app, we can add
